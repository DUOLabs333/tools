output_name

build() for those that do something else

clean as a target

debug_flags

release_flags

clean is a target, but debug is still an env variable

for the actual vulkan-stream

have a base class, where thw only thing that differs is whether CLIENT is 0 or 1

offer PLATFORM, DEBUG, and EXTERNAL_DIR as global variables

classes should just be a child of a base class
